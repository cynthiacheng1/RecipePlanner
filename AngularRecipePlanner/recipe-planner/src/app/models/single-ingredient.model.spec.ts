import { SingleIngredient } from './single-ingredient.model';

describe('SingleIngredient', () => {
  it('should create an instance', () => {
    expect(new SingleIngredient()).toBeTruthy();
  });
});
